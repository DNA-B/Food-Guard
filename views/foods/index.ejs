<h1>음식 목록</h1>

<div class="foods-grid">
  <h2>
    <% foodList.forEach(food => { %>
    <div class="food-card">
      <a href="/foods/<%= food._id %>" class="btn"><%= food.name %></a>
    </div>
    <% }); %>
  </h2>
</div>

<hr />
<a href="/foods/create" class="btn">새 음식 추가</a>
