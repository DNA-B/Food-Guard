<div class="flex flex-col items-center min-h-[60vh] py-8">
  <div class="bg-white shadow-lg rounded-xl p-8 w-full max-w-xl">
    <h1 class="text-3xl font-bold text-indigo-700 mb-8">회원 탈퇴</h1>
    <form action="/users/delete/?_method=DELETE" method="POST" onsubmit="return confirm('정말로 탈퇴하시겠습니까? 이 작업은 되돌릴 수 없습니다.');" class="space-y-6">
      <button type="submit" class="w-full px-5 py-3 bg-red-500 text-white rounded hover:bg-red-600 font-semibold transition">
        탈퇴하기
      </button>
    </form>
  </div>
</div>