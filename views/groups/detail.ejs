<div class="flex flex-col items-center min-h-[60vh] py-8">
  <div class="bg-white shadow-lg rounded-xl p-8 w-full max-w-md">
    <h1 class="text-3xl font-bold text-indigo-700 mb-8"><%= group.name %></h1>
    <p class="text-gray-700 mb-4 text-center">
      <span class="font-semibold">설명</span> : <%= group.description %>
    </p>
    <hr class="mb-6" />
    <div class="flex flex-col gap-2">
      <a
        href="/groups/<%= group._id %>/users"
        class="w-full text-center px-4 py-2 rounded bg-green-100 hover:bg-green-200 text-green-700 font-medium transition"
        >그룹원 목록</a
      >
      <a
        href="/groups/<%= group._id %>/foods"
        class="w-full text-center px-4 py-2 rounded bg-yellow-100 hover:bg-yellow-200 text-yellow-700 font-medium transition"
        >음식 목록</a
      >
      <a
        href="/groups/<%= group._id %>/edit"
        class="w-full text-center px-4 py-2 rounded bg-blue-100 hover:bg-blue-200 text-blue-700 font-medium transition"
        >그룹 수정</a
      >
      <a
        href="/groups/<%= group._id %>/invites/create"
        class="w-full text-center px-4 py-2 rounded bg-orange-100 hover:bg-orange-200 text-orange-700 font-medium transition"
      >
        그룹 초대
      </a>
      <button
        class="w-full text-center px-4 py-2 rounded bg-gray-100 hover:bg-gray-200 text-gray-700 font-medium transition"
        onclick="history.back()"
      >
        목록으로 돌아가기
      </button>
      <form action="/groups/<%= group._id %>/exit" method="POST" class="mt-4">
        <button
          type="submit"
          class="w-full px-4 py-2 rounded bg-red-500 hover:bg-red-600 text-white font-semibold transition"
        >
          그룹 나가기
        </button>
      </form>
    </div>
  </div>
</div>
