<div class="flex flex-col items-center min-h-[60vh] py-8">
  <div class="bg-white shadow-lg rounded-xl p-8 w-full max-w-md">
    <h1 class="text-3xl font-bold text-indigo-700 mb-8">그룹 초대</h1>
    <form
      action="/groups/<%= groupId %>/invites/create"
      method="POST"
      class="space-y-4"
    >
      <div>
        <label for="nickname" class="block text-gray-700 font-medium mb-2"
          >닉네임</label
        >
        <div class="flex gap-4">
          <input
            type="text"
            id="nickname"
            name="nickname"
            required
            class="w-full px-4 py-3 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-indigo-400 text-lg"
          />
          <button
            type="button"
            id="checkNickname"
            class="px-5 py-3 bg-indigo-600 text-white rounded hover:bg-indigo-500 font-semibold transition text-lg whitespace-nowrap"
          >
            중복 검사
          </button>
        </div>
        <span
          id="nicknameMessage"
          class="hidden"
          data-type="invite"
          data-empty="초대할 닉네임을 입력하세요."
          data-exists="초대 가능한 닉네임입니다."
          data-not-exists="해당 닉네임은 존재하지 않습니다."
        ></span>
      </div>

      <div class="flex gap-2 mt-6">
        <button
          type="submit"
          class="w-full px-4 py-2 bg-indigo-600 text-white rounded hover:bg-indigo-500 font-semibold transition opacity-50 cursor-not-allowed"
          disabled
        >
          초대하기
        </button>
        <button
          type="button"
          onclick="history.back()"
          class="w-full px-4 py-2 bg-gray-200 text-gray-700 rounded hover:bg-gray-300 font-semibold transition"
        >
          취소하기
        </button>
      </div>
    </form>
  </div>
</div>

<script src="/javascripts/checkNickname.js"></script>
