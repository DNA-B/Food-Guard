<div class="flex flex-col items-center min-h-[60vh] py-10 w-full">
  <div class="w-full max-w-7xl">
    <h1 class="text-3xl font-bold text-indigo-700 mb-10 text-center">
      그룹 멤버 목록
    </h1>

    <div class="grid grid-cols-2 lg:grid-cols-4 gap-6">
      <% users.forEach(user => { %>
      <div class="bg-gradient-to-br from-blue-50 to-cyan-100 shadow-lg rounded-2xl p-6 hover:shadow-xl transition-all duration-300 hover:scale-105 border border-blue-200 relative">
        <div class="flex flex-col items-center">
          <div class="relative w-16 h-16 mb-3">
            <div class="w-16 h-16 bg-green-100 rounded-full flex items-center justify-center">
              <span class="text-2xl text-green-600 font-bold">
                <%= user.nickname ? user.nickname.charAt(0).toUpperCase() : '?'
                %>
              </span>
            </div>
            <% if (managerId.equals(user._id)) { %>
            <span class="absolute top-0 left-1/2 -translate-x-1/2 -translate-y-1/2 text-xl">
              👑
            </span>
            <% } %>
          </div>
          <h2 class="text-lg font-semibold text-gray-800 mb-2 text-center">
            <%= user.nickname %>
            <span class="text-yellow-600 text-sm ml-1 font-medium">
              <%= managerId.equals(user._id) ? '(매니저)' : '(그룹원)' %>
            </span>
          </h2>
          <p class="text-gray-600 text-sm text-center"><%= user.email %></p>
        </div>
      </div>
      <% }); %>
    </div>
  </div>
</div>